# Client-side Supabase config (safe to expose; RLS guards the data).
# Leave blank to run Qwizzle as a fully local game with accounts hidden.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional table-name overrides (defaults shown).
# VITE_SUPABASE_PROFILES_TABLE=profiles
# VITE_SUPABASE_EDITIONS_TABLE=editions
# VITE_SUPABASE_WORDLISTS_TABLE=wordlists
# VITE_SUPABASE_STATS_TABLE=stats
